#include <iostream>
#include <cmath>
using namespace std;
int main()
{
    // int a, b;
    // this pow function to get a raise to power b
    // cout << "Please enter the value of a:";
    // cin >> a;
    // cout << "Please enter the value of b:";
    // cin >> b;
    // cout << pow(a, b) << endl;

    // This is for sqrt to find the underoot

    double a, b;
    cout << "Please enter the value of a:";
    cin >> a;
    cout << "Please enter the value of b:";
    cin >> b;
    cout << "Value of underoot " << a << " is:" << sqrt(a) << endl;
    cout << "Value of underoot " << b << " is:" << sqrt(b) << endl;
    return 0;
}